#!/usr/bin/env node
const px2rpx = require('../')
const program = require('commander')

program
  .usage('<filename> [options]')
  .option('-c, --cover', 'cover mode')
  .parse(process.argv)

const [filename] = program.args
px2rpx(filename, { cover: program.cover })